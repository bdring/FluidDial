class MacroItem : public Item {
private:
    std::string _filename;

public:
    MacroItem(const char* name) : Item(name) {}
    void invoke(void* arg) override;
    void show(const Point& where) override;
};
